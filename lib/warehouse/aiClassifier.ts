// @ts-nocheck
import { DEFAULTS } from "./utils";

// ShiftLog/TK v1: AI fallback is disabled by default.
// The rule-based classifier still works. Later we can connect this to /api/ai/classify.
export async function classifyWithAI(line, defaults = {}) {
  return null;
}
