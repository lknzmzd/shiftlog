export default function ExpenseForm() {
  return (
    <div className="panel">
      <h2>Expenses</h2>
      <p className="muted">
        In v1 expenses are entered inside the salary calculator. Separate expense
        tracking comes after the core app works.
      </p>
    </div>
  );
}